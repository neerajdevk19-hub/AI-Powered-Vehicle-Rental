# Local mode uses persistent Qdrant storage in ./data/qdrant.
# To use the separate Qdrant server, export QDRANT_URL=http://localhost:6333
# AI_DATA_DIR defaults to this service's data directory.
EMBEDDING_MODEL=sentence-transformers/all-MiniLM-L6-v2
