# Frontend Environment Variables Template (Vite)
# Copy this file to .env or .env.local if you want to override the default backend URL

# Backend API Base URL (Default fallback: http://<current-hostname>:3000)
VITE_API_URL="http://localhost:3000"
